# Setting
set nodrawbox
set icons
set nohidden
set info size
set ifs "\n"
set filesep "\n"
set previewer pv

# Key bindings
map <c-y> up
map <c-e> down
map <enter> open
map gh cd ~
map gr cd /

# Commands
cmd open & open $fx
