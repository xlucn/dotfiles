# options
set mouse
set nodrawbox
set icons
set nohidden
set info size
set scrolloff 99
set ifs "\n"
set filesep "\n"
set errorfmt "\033[31m%s\033[0m"
set previewer pv
set cleaner preview-image

# key mapping
map <esc> cmd-escape
map <c-y> up
map <c-e> down
map <m-up> up
map <m-down> down
map gh cd ~
map gr cd /
map D delete
map zp set preview!
# shell mappings
map <enter> $open $fx
map e $$EDITOR $fx
map M push $mkdir<space>
map R $bulk-rename
map t $tig -C $PWD
