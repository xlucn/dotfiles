# Setting
set nodrawbox
set icons
set hidden
set info size
set ifs "\n"
set filesep "\n"
set previewer pv
set cleaner preview-image

# Key bindings
map <c-y> up
map <c-e> down
map <enter> open
map gh cd ~
map gr cd /

# Commands
cmd open $ open $fx
