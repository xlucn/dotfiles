# Setting
set nodrawbox
set icons
set nohidden
set info size
set ifs "\n"
set filesep "\n"
set previewer ~/.config/lf/pv.sh

# Key bindings
map <c-y> up
map <c-e> down
map <enter> open
map gh cd ~
map gr cd /

# Commands
cmd open $~/.config/lf/open.sh $fx
