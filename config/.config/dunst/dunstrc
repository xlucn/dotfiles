[global]
# Display
monitor = 0
follow = mouse
# geometry
width = (0, 1024)
origin = top-right
offset = 24x72
# progress bar
progress_bar = false
# Show how many messages are currently hidden (because of geometry)
indicate_hidden = true
# Shrink window if it's smaller than the width. Ignored if 0
shrink = true
# The transparency of the window. Range: [0; 100]
transparency = 0

# The height of the entire notification
notification_height = 0
# Separator height between notifications
separator_height = 4

# Vertical padding and horizontal padding
padding = 16
horizontal_padding = 16
frame_width = 4
frame_color = "#749cb3"
separator_color = frame

# Sort messages by urgency
sort = false
# Don't remove messages, if the user is idle for longer than idle_threshold seconds
idle_threshold = 120
# Font, This options is parsed as a Pango font description.
font = Monospace 12
# Line height of texts
line_height = 0
# Possible values are: full, strip, no
markup = no
# The format of the message.  Possible variables are:
#   %a  appname
#   %s  summary
#   %b  body
#   %p  progress value if set ([  0%] to [100%]) or nothing
#   %n  progress value if set without any extra characters
#   %%  Literal %
# Markup is allowed
format = "%a\n<b>%s</b> %p\n%b"

# Alignment of message text
# Possible values are "left", "center" and "right"
alignment = left
vertical_alignment = center
# Show age of message if message is older than show_age_threshold seconds
show_age_threshold = 60
# Split notifications into multiple lines if they don't fit into geometry
word_wrap = true
# For truncated lines
ellipsize = middle
# Ignore newlines '\n' in notifications
ignore_newline = false
# Stack together notifications with the same content
stack_duplicates = true
# Hide the count of stacked notifications with the same content
hide_duplicate_count = false
# Display indicators for URLs (U) and actions (A)
show_indicators = true

# Align icons left/right/off
icon_position = left
# Scale larger icons up/down to this size, set max to 0 to disable
min_icon_size = 64
max_icon_size = 256

# Paths to default icons
# for theme in /usr/share/icons/*; do grep "Directories" "$theme//index.theme" | tr ',=' '\n' | while read -r dir; do [ -d "$theme/$dir" ] && [ ! -h "$theme/$dir" ] && [ ! -h "$theme/${dir%/*}" ] && echo "$theme/$dir"; done ; done
icon_path = /usr/share/icons/Papirus-Dark/symbolic/status

### History ###

# Let history be sticky or timeout as if it would normally do
sticky_history = true
# Maximum amount of notifications kept in history
history_length = 200

### Misc/Advanced ###
# dmenu path
dmenu = rofi -dmenu -p dunst:
# Browser for opening urls in context menu
browser = /usr/bin/firefox -new-tab
# Always run rule-defined scripts, even if the notification is suppressed
always_run_script = true

# X11
title = Dunst
class = Dunst
# Print a notification on startup for error detection
startup_notification = false
# Manage dunst's desire for talking: crit warn mesg info debug
verbosity = mesg
# Legacy
force_xinerama = false
# Rounded corners
corner_radius = 0

# Defines action of mouse event
mouse_left_click = close_current
mouse_middle_click = close_all
mouse_right_click = do_action

[urgency_low]
background = "#212121"
foreground = "#888888"
timeout = 10

[urgency_normal]
background = "#212121"
foreground = "#ffffff"
timeout = 10

[urgency_critical]
background = "#ff5252"
foreground = "#ffffff"
frame_color = "#ff5252"
timeout = 0

[dwm-status]
category = "dwm-status"
set_stack_tag = "dwm-status"

# vim: ft=cfg
