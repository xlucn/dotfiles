#!/bin/sh
if [ -f config.def.h ]; then
    make clean && make && make PREFIX=~/.local/ install && make clean
else
    rm -f config.h && make && make PREFIX=~/.local/ install && make clean && rm config.h
fi
