#!/bin/bash
# shellcheck disable=SC1090
[ -f ~/.profile ] && . ~/.profile
# shellcheck disable=SC1090
[ -f ~/.bashrc ] && . ~/.bashrc
# shellcheck disable=SC1090
[ -f ~/.extend.bash_profile ] && . ~/.extend.bash_profile
